[package]
name = "orderbook"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Order book data structures for the trading system"

[dependencies]
types = { path = "../types" }
serde = { workspace = true }
borsh = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
serde_json = "1.0"

[lints]
workspace = true
